name: Text-to-Image Web App

on:
  workflow_dispatch:
    inputs:
      prompt:
        description: 'Default prompt for image generation'
        required: true
        default: 'A beautiful sunset over mountains, digital art'

jobs:
  text-to-image-app:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Python dependencies
      run: |
        cd web-app
        pip install -r requirements.txt

    - name: Install Ngrok
      run: |
        wget -q -O ngrok.tgz https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
        tar -xzf ngrok.tgz
        chmod +x ngrok
        sudo mv ngrok /usr/local/bin/
        ngrok version

    - name: Setup Ngrok authentication
      run: |
        ngrok authtoken "${{ secrets.NGROK_AUTH_TOKEN }}"
        echo "✅ Ngrok authentication configured"

    - name: Create outputs directory
      run: mkdir -p web-app/outputs

    - name: Start Flask server with wait
      run: |
        echo "Starting Flask server..."
        cd web-app
        python app.py &
        SERVER_PID=$!
        echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
        
        # Wait for server to be ready
        echo "Waiting for server to start..."
        for i in {1..30}; do
          if curl -s http://localhost:5000/health > /dev/null 2>&1; then
            echo "✅ Flask server is ready on port 5000"
            break
          fi
          if [ $i -eq 30 ]; then
            echo "❌ Server failed to start"
            exit 1
          fi
          sleep 2
        done

    - name: Start Ngrok tunnel
      run: |
        echo "Starting Ngrok tunnel..."
        ngrok http 5000 --log=stdout > ngrok.log 2>&1 &
        echo "NGROK_PID=$!" >> $GITHUB_ENV
        
        # Wait for ngrok to be ready
        sleep 10

    - name: Get Ngrok public URL
      id: get_url
      run: |
        echo "Getting Ngrok public URL..."
        max_attempts=10
        for i in $(seq 1 $max_attempts); do
          if curl -s http://localhost:4040/api/tunnels > /dev/null 2>&1; then
            NGROK_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url // empty' 2>/dev/null || echo "")
            if [ -n "$NGROK_URL" ] && [ "$NGROK_URL" != "null" ]; then
              echo "✅ Ngrok URL: $NGROK_URL"
              echo "ngrok_url=$NGROK_URL" >> $GITHUB_OUTPUT
              echo "NGROK_PUBLIC_URL=$NGROK_URL" >> $GITHUB_ENV
              break
            fi
          fi
          echo "Attempt $i: Waiting for Ngrok..."
          sleep 3
        done

        if [ -z "$NGROK_PUBLIC_URL" ]; then
          echo "❌ Could not get Ngrok URL"
          echo "Ngrok logs:"
          cat ngrok.log
          exit 1
        fi

    - name: Display connection information
      run: |
        echo ""
        echo "✨ ========================================="
        echo "✨   AI IMAGE GENERATOR READY!"
        echo "✨ ========================================="
        echo "✨ Web Interface: $NGROK_PUBLIC_URL"
        echo "✨ Health Check: $NGROK_PUBLIC_URL/health"
        echo "✨ ========================================="

    - name: Keep service running
      run: |
        echo "🕐 AI Image Generator active for 25 minutes..."
        echo "🌐 Your URL: $NGROK_PUBLIC_URL"
        echo ""
        echo "📊 Monitoring service..."
        
        for i in {1..150}; do
          minutes=$((i/6))
          seconds=$((i%6*10))
          echo "[$(date +%H:%M:%S)] Active: ${minutes}m ${seconds}s"
          
          # Check server health every minute
          if [ $((i % 6)) -eq 0 ]; then
            if curl -s http://localhost:5000/health > /dev/null; then
              echo "   ✅ Server: Healthy"
            else
              echo "   ❌ Server: Down"
            fi
          fi
          
          sleep 10
        done

    - name: Upload generated images
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ai-images
        path: web-app/outputs/
        retention-days: 30
        if-no-files-found: warn

    - name: Cleanup
      if: always()
      run: |
        echo "🧹 Cleaning up..."
        kill $SERVER_PID 2>/dev/null || true
        pkill -f ngrok 2>/dev/null || true
        echo "✅ Cleanup complete"
