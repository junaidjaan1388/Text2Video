- name: Generate via API
  run: |
    pip install requests
    python -c "
    import requests
    # Use free image APIs instead of local models
    response = requests.get('https://picsum.photos/512/512')
    with open('outputs/image.jpg', 'wb') as f:
        f.write(response.content)
    print('Image downloaded')
    "
