- name: Get Ngrok public URL
  id: ngrok_url
  run: |
    echo "🔗 Setting up Ngrok access..."
    echo ""
    echo "📋 MANUAL ACCESS INSTRUCTIONS:"
    echo "================================"
    echo "1. Go to https://dashboard.ngrok.com/cloud-edge/endpoints"
    echo "2. Find your active tunnel"
    echo "3. Copy the public URL (ends with .ngrok.io)"
    echo "4. Use that URL to access your AI Image Generator"
    echo "================================"
    echo ""
    echo "For automatic setup, ensure:"
    echo "✅ NGROK_AUTH_TOKEN is set in GitHub Secrets"
    echo "✅ Token is valid and not expired"
    echo "✅ No firewall blocking Ngrok"
    
    # Provide a placeholder
    echo "ngrok_url=https://dashboard.ngrok.com/endpoints" >> $GITHUB_OUTPUT
